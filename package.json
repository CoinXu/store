{
  "name": "sugo-store",
  "version": "0.2.4",
  "description": "state manager",
  "author": "CoinXu<duanxian0605@gmail.com>",
  "repository": "https://github.com/CoinXu/store",
  "bugs": {
    "url": "https://github.com/CoinXu/store/issues"
  },
  "keywords": [
    "store",
    "state manager",
    "multiple dispatch"
  ],
  "scripts": {
    "clean:build": "rm -rf ./lib/src/*",
    "build": "npm run clean:build && ./node_modules/.bin/tsc",
    "clean:test": "rm -rf ./lib/__test__/*",
    "build:test": "npm run clean:test && ./node_modules/.bin/tsc",
    "test": "npm run build:test && ./node_modules/.bin/mocha ./lib/__test__",
    "prepublishOnly": "npm run build && npm run test"
  },
  "main": "./lib/src",
  "typings": "./@types/src",
  "license": "MIT",
  "devDependencies": {
    "typescript": "2.6.1",
    "mocha": "^3.3.0",
    "@types/mocha": "^2.2.44",
    "@types/node": "8.0.53"
  }
}